import freeEducationMetadata from "@/components/freeEducation/freeEducationMetadata";

export const metadata = freeEducationMetadata;

export default function FreeEducationLayout({ children }) {
  return children;
}